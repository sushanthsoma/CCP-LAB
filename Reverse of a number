#include<stdio.h>
int main()
{
    int n,r=0,t;
    printf("Enter number\n");
    scanf("%d",&n);
    while(n>0)
    {
        t=n%10;
        r=r*10+t;
        n=n/10;
    }
    printf("reverse=%d\n",r);
    return 0;
}
